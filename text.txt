docker image prune --all

docker-compose -f docker-compose-build.yaml build --parallel

docker-compose up

docker run -p 5432:5432 -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=zaQ@1234 -e POSTGRES_DB=postgres -d postgres
